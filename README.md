After knitting and rendering `EasternCanadaHydrology.Rmd`, overwrite this file with a symlink to `EasternCanadaHydrology.md`.

E.g., on Linux/macOS from terminal:

```bash
cd E:/EasternCanadaHydrology/modules/EasternCanadaHydrology
rm README.md && ln -s EasternCanadaHydrology.md README.md
```
